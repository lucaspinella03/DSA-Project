#include <iostream>
#include "simulation.h"


int main() {

    Simulation sim1;
    //sim1.printInformation();

    sf::RenderWindow mainWindow(sf::VideoMode(640, 480), "SFML Test");

    while(mainWindow.isOpen()){

    }

    return 0;

}
